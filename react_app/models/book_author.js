class BookAuthor {
    constructor(id, bookId, authorId, createdAt, updatedAt) {
      this.id = id;
      this.bookId = bookId;
      this.authorId = authorId;
      this.createdAt = createdAt;
      this.updatedAt = updatedAt;
    }
  }
  
  export default BookAuthor;
  