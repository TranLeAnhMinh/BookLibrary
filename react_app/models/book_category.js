class BookCategory {
    constructor(id, bookId, categoryId, createdAt, updatedAt) {
      this.id = id;
      this.bookId = bookId;
      this.categoryId = categoryId;
      this.createdAt = createdAt;
      this.updatedAt = updatedAt;
    }
  }
  
  export default BookCategory;
  