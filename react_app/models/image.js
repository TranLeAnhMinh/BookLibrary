class Image {
    constructor(id, url, bookId, createdAt, updatedAt, deletedAt) {
      this.id = id;
      this.url = url;
      this.bookId = bookId;
      this.createdAt = createdAt;
      this.updatedAt = updatedAt;
      this.deletedAt = deletedAt;
    }
  }
  
  export default Image;
  