class Publisher {
    constructor(id, name, description, slug, createdAt, updatedAt, deletedAt) {
      this.id = id;
      this.name = name;
      this.description = description;
      this.slug = slug;
      this.createdAt = createdAt;
      this.updatedAt = updatedAt;
      this.deletedAt = deletedAt;
    }
  }
  
  export default Publisher;
  